name: Build and Push Docker Images

on:
  push:
    branches: [ "master" ]

env:
  REGISTRY: ghcr.io

jobs:
  discover:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      has-images: ${{ steps.set-matrix.outputs.has-images }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Discover images
        id: set-matrix
        run: |
          echo "🔍 Discovering images in images/ directory..."
          
          if [ ! -d "images" ]; then
            echo "❌ images/ directory not found"
            echo "matrix=[]" >> $GITHUB_OUTPUT
            echo "has-images=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          images_array=()
          for dir in images/*/; do
            if [ -d "$dir" ]; then
              image_name=$(basename "$dir")
              dockerfile="$dir/Dockerfile"
              tagfile="$dir/tag"
              
              if [ ! -f "$dockerfile" ]; then
                echo "⚠️  Skipping $image_name: Dockerfile not found"
                continue
              fi
              
              if [ ! -f "$tagfile" ]; then
                echo "⚠️  Skipping $image_name: tag file not found"
                continue
              fi
              
              images_array+=("$image_name")
              echo "✅ Found: $image_name"
            fi
          done
          
          if [ ${#images_array[@]} -eq 0 ]; then
            echo "⚠️  No valid images found"
            echo "matrix=[]" >> $GITHUB_OUTPUT
            echo "has-images=false" >> $GITHUB_OUTPUT
          else
            matrix_json=$(printf '%s\n' "${images_array[@]}" | jq -R . | jq -s -c .)
            echo "matrix=$matrix_json" >> $GITHUB_OUTPUT
            echo "has-images=true" >> $GITHUB_OUTPUT
            echo "📦 Total images to process: ${#images_array[@]}"
          fi

  build:
    needs: discover
    runs-on: ubuntu-latest
    if: needs.discover.outputs.has-images == 'true'
    
    strategy:
      matrix:
        image: ${{ fromJson(needs.discover.outputs.matrix) }}
      fail-fast: false
      max-parallel: 3
    
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare image name (lowercase)
        id: prepare
        run: |
          IMAGE_NAME="${{ matrix.image }}"
          IMAGE_NAME_LOWER=$(echo "$IMAGE_NAME" | tr '[:upper:]' '[:lower:]')
          REPO_OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          
          echo "image_name=$IMAGE_NAME_LOWER" >> $GITHUB_OUTPUT
          echo "repo_owner=$REPO_OWNER" >> $GITHUB_OUTPUT
          echo "✅ Image name: $IMAGE_NAME_LOWER"
          echo "✅ Repository owner: $REPO_OWNER"

      - name: Validate image structure
        id: validate
        run: |
          IMAGE_NAME="${{ matrix.image }}"
          IMAGE_DIR="images/${IMAGE_NAME}"
          DOCKERFILE="${IMAGE_DIR}/Dockerfile"
          TAG_FILE="${IMAGE_DIR}/tag"
          
          echo "🔍 Validating image: $IMAGE_NAME"
          
          if [ ! -f "$DOCKERFILE" ]; then
            echo "❌ Dockerfile not found: $DOCKERFILE"
            exit 1
          fi
          
          if [ ! -f "$TAG_FILE" ]; then
            echo "❌ Tag file not found: $TAG_FILE"
            exit 1
          fi
          
          IMAGE_TAG=$(cat "$TAG_FILE" | tr -d '[:space:]')
          if [ -z "$IMAGE_TAG" ]; then
            echo "❌ Tag file is empty"
            exit 1
          fi
          
          IMAGE_TAG_LOWER=$(echo "$IMAGE_TAG" | tr '[:upper:]' '[:lower:]')
          
          if [[ ! "$IMAGE_TAG_LOWER" =~ ^[a-z0-9._-]+$ ]]; then
            echo "❌ Invalid tag format: $IMAGE_TAG_LOWER"
            exit 1
          fi
          
          echo "image_tag=$IMAGE_TAG_LOWER" >> $GITHUB_OUTPUT
          echo "✅ Tag validated: $IMAGE_TAG_LOWER"

      - name: Check if image tag exists in registry
        id: check-tag
        continue-on-error: true
        run: |
          IMAGE_NAME="${{ steps.prepare.outputs.image_name }}"
          IMAGE_TAG="${{ steps.validate.outputs.image_tag }}"
          REPO_OWNER="${{ steps.prepare.outputs.repo_owner }}"
          FULL_IMAGE="${{ env.REGISTRY }}/${REPO_OWNER}/${IMAGE_NAME}:${IMAGE_TAG}"
          
          echo "🔍 Checking registry for: $FULL_IMAGE"
          
          # Use GitHub Container Registry API
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://${{ env.REGISTRY }}/v2/${REPO_OWNER}/${IMAGE_NAME}/manifests/${IMAGE_TAG}")
          
          if [ "$RESPONSE" = "200" ]; then
            echo "✅ Image tag already exists in registry"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "🔨 Image tag not found in registry (HTTP $RESPONSE)"
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Skip notification
        if: steps.check-tag.outputs.skip == 'true'
        run: |
          echo "⏭️ Skipping build for ${{ steps.prepare.outputs.image_name }}:${{ steps.validate.outputs.image_tag }}"
          echo "Image already exists in registry"

      # Set up QEMU for multi-platform builds
      - name: Set up QEMU
        if: steps.check-tag.outputs.skip != 'true'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: linux/amd64,linux/arm64

      # Set up Docker Buildx with proper configuration
      - name: Set up Docker Buildx
        if: steps.check-tag.outputs.skip != 'true'
        uses: docker/setup-buildx-action@v3
        with:
          version: latest
          driver-opts: |
            image=moby/buildkit:latest
            network=host
          buildkitd-flags: --debug

      - name: Log into registry ${{ env.REGISTRY }}
        if: steps.check-tag.outputs.skip != 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        if: steps.check-tag.outputs.skip != 'true'
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.prepare.outputs.repo_owner }}/${{ steps.prepare.outputs.image_name }}
          tags: |
            type=raw,value=${{ steps.validate.outputs.image_tag }}
            type=raw,value=latest

      # Build and push multi-platform image
      - name: Build and push Docker image
        if: steps.check-tag.outputs.skip != 'true'
        id: build-and-push
        uses: docker/build-push-action@v5
        with:
          context: ./images/${{ matrix.image }}
          file: ./images/${{ matrix.image }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Multi-platform build
          platforms: linux/amd64,linux/arm64
          # Disable attestations to avoid manifest issues
          provenance: false
          sbom: false
          # Proper output configuration
          outputs: type=image,push=true
          # Cache configuration
          cache-from: type=gha,scope=${{ steps.prepare.outputs.image_name }}
          cache-to: type=gha,mode=max,scope=${{ steps.prepare.outputs.image_name }}

      - name: Verify multi-platform image
        if: steps.check-tag.outputs.skip != 'true'
        run: |
          IMAGE_NAME="${{ steps.prepare.outputs.image_name }}"
          IMAGE_TAG="${{ steps.validate.outputs.image_tag }}"
          REPO_OWNER="${{ steps.prepare.outputs.repo_owner }}"
          FULL_IMAGE="${{ env.REGISTRY }}/${REPO_OWNER}/${IMAGE_NAME}:${IMAGE_TAG}"
          
          echo "🔍 Verifying multi-platform image: $FULL_IMAGE"
          
          # Wait for registry to sync
          sleep 10
          
          # Check manifest
          echo "📋 Manifest details:"
          docker buildx imagetools inspect "$FULL_IMAGE"
          
          echo ""
          echo "✅ Image built for platforms:"
          docker buildx imagetools inspect "$FULL_IMAGE" --raw | jq -r '.manifests[].platform | "\(.os)/\(.architecture)"'

      - name: Install cosign
        if: steps.check-tag.outputs.skip != 'true'
        uses: sigstore/cosign-installer@v3.5.0
        with:
          cosign-release: 'v2.2.4'

      - name: Sign the published Docker image
        if: steps.check-tag.outputs.skip != 'true'
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
        run: |
          echo "🔐 Signing images..."
          echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}

      - name: Build summary
        if: always()
        run: |
          echo "## Build Summary for ${{ steps.prepare.outputs.image_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.check-tag.outputs.skip }}" = "true" ]; then
            echo "⏭️ **Status:** Skipped (image already exists)" >> $GITHUB_STEP_SUMMARY
            echo "- **Image:** ${{ steps.prepare.outputs.image_name }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Tag:** ${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ steps.build-and-push.outcome }}" = "success" ]; then
            echo "✅ **Status:** Successfully built and pushed" >> $GITHUB_STEP_SUMMARY
            echo "- **Image:** ${{ steps.prepare.outputs.image_name }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Tag:** ${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Digest:** ${{ steps.build-and-push.outputs.digest }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Platforms:** linux/amd64, linux/arm64" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Pull Commands" >> $GITHUB_STEP_SUMMARY
            echo '```bash' >> $GITHUB_STEP_SUMMARY
            echo "# Pull for your platform (auto-detect)" >> $GITHUB_STEP_SUMMARY
            echo "docker pull ${{ env.REGISTRY }}/${{ steps.prepare.outputs.repo_owner }}/${{ steps.prepare.outputs.image_name }}:${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "# Pull specific platform" >> $GITHUB_STEP_SUMMARY
            echo "docker pull --platform linux/amd64 ${{ env.REGISTRY }}/${{ steps.prepare.outputs.repo_owner }}/${{ steps.prepare.outputs.image_name }}:${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
            echo "docker pull --platform linux/arm64 ${{ env.REGISTRY }}/${{ steps.prepare.outputs.repo_owner }}/${{ steps.prepare.outputs.image_name }}:${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Inspect Manifest" >> $GITHUB_STEP_SUMMARY
            echo '```bash' >> $GITHUB_STEP_SUMMARY
            echo "docker buildx imagetools inspect ${{ env.REGISTRY }}/${{ steps.prepare.outputs.repo_owner }}/${{ steps.prepare.outputs.image_name }}:${{ steps.validate.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Status:** Build failed" >> $GITHUB_STEP_SUMMARY
            echo "- **Image:** ${{ steps.prepare.outputs.image_name }}" >> $GITHUB_STEP_SUMMARY
          fi

  summary:
    needs: [discover, build]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Workflow summary
        run: |
          echo "## Workflow Complete 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All image builds have been processed." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Multi-Platform Support" >> $GITHUB_STEP_SUMMARY
          echo "Images are built for:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ linux/amd64 (x86_64)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ linux/arm64 (ARM64/Apple Silicon)" >> $GITHUB_STEP_SUMMARY
